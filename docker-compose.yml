services:
  civicflow:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4173:4173"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:4173 || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 15s
